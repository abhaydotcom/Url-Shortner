<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body{
            font-family: 'Times New Roman', Times, serif;
            background: #000;
            color: aliceblue;
        }

    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SSR</title>
</head>
<body>
    <h1>Url Shortner </h1>
    <% if(locals.id){%>
        <p>URL Generated: http://localhost:9000/url/<%=id %></p>
        <%}%>

    <div>
        <form action="/url" method="post">
            <label >Enter Your Original URL</label>
            <input type="text" placeholder="http://example.co" name="url">

            <button type="submit">Generate</button>
        </form>
    </div>
<br>
    <div>
        <% if(locals.urls){%>
        
            <table>
                <thead>
                    <th>S.No</th>
                    <th>ShortId</th>
                    <th>Redirects</th>
                    <th>Clicks</th>
                </thead>
                <tbody>
                    <% urls.forEach((url,index) => { %>
                        <tr>
                     <td><%= index+1  %></td>
                     <td><%= url.shortId%></td>
                     <td><%= url.redirectUrl %></td>
                     <td><%= url.visitHistory.length %></td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        <%} %>
    </div>
    
</body>
</html>